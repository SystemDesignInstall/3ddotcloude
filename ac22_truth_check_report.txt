WORKING TREE — строки, упоминающие AC-22: MILESTONE + REPORT
##### P3-milestone-status.md (working)
   136 | - **Step 11 (failure/recovery N1–N6 through `Engine::RunPipeline` ONLY, PROVEN):** the milestone's negative/recovery acceptance, all entered through the same surface with NO direct host-stage calls. **N1 damage
   166 |   AC-22 (PROVEN 2026-09-12: `AC22_DeterministicSecondRunThroughProductionSurface` twin — two fresh

##### P3.1-report (working)
   225 | | AC-22: Deterministic second run | Twin double-run via `Engine::RunPipeline` (two fresh projects, real GTSAM seams), Debug EXIT 0 + Release EXIT 0 | PROVEN |

==============================
git diff --check:
  exit_out=''  stderr=''

git status --short:
   M docs/architecture/P3-milestone-status.md
   M docs/architecture/P3.1-production-sparse-correction-verification-report.md
   M tests/CMakeLists.txt
   M tests/unit/test_p3_trajectory_optimization.cpp
  ?? ac22_both_docs_full.txt
  ?? ac22_commit_preview_gen.py
  ?? ac22_docs_diff_utf8.txt
  ?? ac22_dump_diffs.py
  ?? ac22_milestone.diff
  ?? ac22_report.diff
  ?? ac22_report_utf8.diff.txt
  ?? cli_preview_milestone.diff
  ?? cli_preview_milestone.diff.txt
  ?? cli_preview_report.diff
  ?? cli_preview_report.diff.txt
  ?? gen_ac22_docs_diff_utf8.py
  ?? milestone_hunks_gen.py
  ?? ms_diff_here.diff
  ?? rep_diff_here.diff
  ?? tests/unit/test_cli_sparse_correction_e2e.cpp
  ?? tmp_ac22_hunklines.py
  ?? tmp_ac22_preview.py
  ?? tmp_dump_milestone_utf8.py

git diff --stat:
   docs/architecture/P3-milestone-status.md           |  33 +
   ...uction-sparse-correction-verification-report.md |   2 +-
   tests/CMakeLists.txt                               |  53 +-
   tests/unit/test_p3_trajectory_optimization.cpp     | 798 +++++++++++++++++++++
   4 files changed, 857 insertions(+), 29 deletions(-)

РЕПОРТ: полный hunk-сплит diff (малый файл) — ищем есть ли НЕ-AC-22 +/- строки
  HUNK @@ -222,7 +222,7 @@ This table preserves  add=1 del=1  NON-AC22-lines=0
     + | AC-22: Deterministic second run | Twin double-run via `Engine::RunPipeline` (two fresh projects, real GTSAM seams), Debug EXIT 0 + Release EXIT 0 | PROVEN |
     - | AC-22: Deterministic second run | No double-run | NOT PROVEN |

MILESTONE: полный hunk-сплит diff — отделить Step-12-хунки от AC-22-хунков
  HUNK @@ -134,6 +134,39 @@ CAS + Provenance  add=33 del=0  AC22=True Step12=True CLI=True
     + - **Step 12 (CLI real providers + CLI E2E through the SOLE user entry point, PROVEN):**  the CLI
     +   composition root injects the REAL seam implementations вЂ” `L2NearestMatcher` +
     +   `EssentialGeometricVerifier` + `GtsamTrajectoryOptimizer` + `GtsamBundleAdjustmentOptimizer`
     +   (owned by the engine handle so the seam objects outlive the in-process runner's value-captured
     +   pointers; code committed at HEAD `fbef42b` alongside the adapter links in `cli/CMakeLists.txt`,
     +   the `--input <file>`/`--config <json>` run path, and the exit code вЂ” `RunCommand` returns 0 ONLY
     +   on a succeeded manifest). GTSAM's runtime DLLs (tbb12/tbbmalloc) are copied post-build next to
     +   `spatial.exe` so every child-spawned CLI works without a manual PATH. New
     +   `tests/unit/test_cli_sparse_correction_e2e.cpp` (`spatial_cli_sparse_correction_e2e_tests`, 2
     +   tests, discovery) spawns the REAL binary `spatial run p3_sparse_correction --project <dir>
     +   --input <recon.json> --config <json>` and never calls internal stages directly:
     +   **positive** вЂ” exit 0, manifest "succeeded" with BOTH stages succeeded, and the FULL v4 chain