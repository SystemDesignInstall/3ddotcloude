// P3-impl-7a: classical visual matcher adapter translation unit.
// The adapter is header-only (visual_matcher_adapter.h); this TU exists so the
// adapter has a stable, linkable library identity, mirroring how other adapters
// present a library seam (RFC-0008 §5).

#include "adapters/visual_matching/visual_matcher_adapter.h"
